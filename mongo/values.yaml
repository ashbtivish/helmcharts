# Default values for mongo.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: docker.io/mongo:latest
  pullPolicy: IfNotPresent

application:
  name: "mongo-test"
  fullname: "mongo-test-ashish"
  port: 27017

labels:
  app.kubernetes.io/instance: mongo-ashish

nameOverride: "mongo-gcp"
fullnameOverride: "mongo-gcp-ashish"

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  path: /
  hosts:
    - mongo.gcp
  tls: 
    base64EncodedPrivateKey: null
    base64EncodedCertificate: null
    #- secretName: chart-example-tls
    #  hosts:
    #    - mongo.gcp

resources: 
#limits:
#    cpu: 100m
#    memory: 128Mi
#  requests:
#    cpu: 100m
#    memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}

